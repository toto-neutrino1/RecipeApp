<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/main_background_color">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginHorizontal="@dimen/horizontal_margin_main"
        android:layout_marginBottom="@dimen/bottom_margin"
        android:orientation="horizontal">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/horizontal_margin_supplementary_half"
            android:layout_weight="1"
            android:backgroundTint="@color/btn_categories_color"
            style="@style/StyleNavButton"
            android:text="@string/title_categories"/>
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/horizontal_margin_supplementary_half"
            android:layout_weight="1"
            app:icon="@drawable/ic_heart_empty"
            app:iconGravity="end"
            android:backgroundTint="@color/btn_favorites_color"
            style="@style/StyleNavButton"
            android:text="@string/title_favorites"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>